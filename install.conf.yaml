- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.zshenv:
    # ~/.gitconfig:
    ~/.doom.d:
    ~/.oh-my-zsh/custom: ~/.dotfiles/oh-my-zsh
    ~/.p10k.zsh:
    ~/.mbsyncrc:
    ~/.msmtprc:
    ~/.wallpaper:
    ~/.yabairc:
    ~/.skhdrc:
    ~/.config/karabiner: ~/.dotfiles/karabiner
    ~/.ideavimrc:
    # ~/dev/obsidian_vault: ~/.dotfiles/obsidian
    # ~/.dotfiles/org: /Users/eerickson/Library/Mobile\ Documents/iCloud\~com\~appsonthemove\~beorg/Documents/org
    # /Applications: ~/.dotfiles/scripts/Flow\ -\ Start.app/

- create:
    - ~/inbox/
    - ~/.maildir/
    - ~/.maildir/icloud/
    - ~/.maildir/evan.erksn-gmail/
    - ~/.maildir/ericenna-gmail/
    - ~/.maildir/work/
    # - ~/downloads
    # - ~/.vim/undo-history

- shell:
    - command: ./setup-homebrew.zsh
      stdout: true
      stderr: true
    - command: ./setup-doom.zsh
      stdout: true
      stderr: true
    # - command: ./setup-mu4e.zsh
    #   stdout: true
    #   stderr: true
